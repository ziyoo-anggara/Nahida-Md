import os from 'os';
import fs from 'fs';

export default {
  id: ['ddos'],
  run: async ({ client, m, username, runtime, userdb, command }) => {
     m.reply("*</> Request berhasil, Mengirim 100jt Request Pada web 😈....*")
  }
}